name: 'Swift Test'
description: 'Tests a Vapor application'
inputs:
  version:
    description: 'Swift version to test'
    required: true
runs:
  using: 'docker'
  image: 'docker://vapor/swift:${{ inputs.version }}'
  args:
    - test
